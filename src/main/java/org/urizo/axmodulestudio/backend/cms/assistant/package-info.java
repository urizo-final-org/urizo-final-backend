/**
 * Natural-language CMS boundary for feature 5.
 *
 * <p>This package intentionally contains no runtime component until the feature is assigned.
 * Future code may call CMS services and the shared AI integration, while the base CMS must not
 * depend on this optional assistant boundary.
 */
package org.urizo.axmodulestudio.backend.cms.assistant;
